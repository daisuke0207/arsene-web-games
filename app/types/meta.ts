export type WebsiteMeta = {
  pathname: string;
  title?: string;
  description?: string;
  image?: string;
  keywords?: Array<string>;
};

export type JsonLd = {
  pathname: string;
  description?: string;
  image?: string;
  keywords?: Array<string>;
};
